# Port the HTTP server listens on.
PORT=8080

# Secret key used to sign and verify JWTs. Set this to a long, random
# value in any real deployment — do not use the default.
JWT_SECRET=change-this-to-a-long-random-secret
